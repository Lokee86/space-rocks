info:
  name: logout
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{base_url}}/auth/logout"
  headers:
    - name: Authorization
      value: Bearer {{auth_token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
