info:
  name: me
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/auth/me"
  headers:
    - name: Authorization
      value: Bearer {{auth_token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
