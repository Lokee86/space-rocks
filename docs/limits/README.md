# Current Limits

`docs/limits` contains factual current limitations, unavailable features, incomplete integrations, hardcoded fallbacks, and current constraints.

Use this folder for what the system can and cannot do right now. Design docs should only point here with a short `Related Limits` link when needed to prevent misuse.
